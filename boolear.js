const firstNumber = 85
const secondNumber = 38
if (firstNumber > secondNumber)
    {console.log (`верно, число ${firstNumber} больше числа ${secondNumber}`)}
else {console.log (`неверно, число ${firstNumber} меньше числа ${secondNumber}`)}